<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:paddingTop="8dp">

    <TextView
        android:id="@+id/message_body_text"
        android:text="Hi, I'm fine!"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="8dp"
        android:maxWidth="240dp"
        android:padding="6dp"
        android:textColor="@android:color/black"
        android:background="@drawable/rounded_rectangle_sent"/>

    <TextView
        android:id="@+id/message_time_text"
        android:text="14:00"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintRight_toLeftOf="@id/message_body_text"
        app:layout_constraintBottom_toBottomOf="@id/message_body_text"
        android:layout_marginLeft="4dp"
        android:textSize="10sp" />

</androidx.constraintlayout.widget.ConstraintLayout>